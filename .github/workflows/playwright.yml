name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Ensure browsers and OS dependencies are installed
      # We add 'xvfb' to the system install to provide a virtual display buffer
      run: |
        sudo apt-get update
        sudo apt-get install -y xvfb
        playwright install --with-deps

    - name: Run Playwright tests
      run: |
        mkdir -p reports
        # xvfb-run -a creates a virtual screen (:99) so Firefox doesn't crash 
        # looking for a DISPLAY environment variable.
        xvfb-run -a pytest --browser chromium --browser firefox

    - name: Upload Test Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: pytest-report
        path: reports/report.html